// Enums partilhados pelos modelos Dart do Padeleiro MVP.

enum UserStatus { pending, active, suspended, rejected }

enum MatchStatus { scheduled, completed }

enum SyncStatus { pending, synced, error }
